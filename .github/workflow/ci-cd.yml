name: CI-CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend
        run: |
          docker build -t yourusername/hms-backend ./backend
          docker push yourusername/hms-backend

      - name: Build & Push Frontend
        run: |
          docker build -t yourusername/hms-frontend ./frontend
          docker push yourusername/hms-frontend
