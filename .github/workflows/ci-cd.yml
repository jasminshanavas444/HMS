name: CI-CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend
        run: |
          docker build -t jasminshanavas/hms-backend:latest ./backend
          docker push jasminshanavas/hms-backend:latest

      - name: Build & Push Frontend
        run: |
          docker build -t jasminshanavas/hms-frontend:latest ./frontend
          docker push jasminshanavas/hms-frontend:latest

